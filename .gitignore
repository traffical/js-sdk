node_modules/
dist/
*.tgz

# Generated at build time from package.json (scripts/generate-version.ts).
# Committing it lets the stamped sdkVersion drift out of sync with the
# published version — always regenerate instead.
packages/*/src/version.ts
.DS_Store
*.log
.env
.env.*
.turbo
coverage/
.nyc_output/
.svelte-kit/

# npm auth - never commit!
.npmrc

